/**
 * Importing this module registers every object kind. Import it once, at app
 * startup, before anything reads the registry.
 */
import './structures'
import './features'
import './plants'
import './furniture'
import './boundaries'
